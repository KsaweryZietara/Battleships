# Battleships

Battleships console game created in c#.

## Running the app
To build and run app:

1. Go to the app folder and build:

    ```console
    dotnet build
    ```

2. Run app:

    ```console
    dotnet run
    ```
